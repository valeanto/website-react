import React, {useState, useMemo} from 'react'
// useMemo caches an input value and given output so that it doesnt have to render every time app renders
// two pieces of state, number and color
// functions that doubles our number
// loop
export default function useMemoTest() {
    return (
        <div>
            
            
        </div>
    )
}
